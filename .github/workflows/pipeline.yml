name: ci
on: 
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  pipe:
    concurrency:
      group: ${{ github.ref }}
      cancel-in-progress: true
    uses: platform-mesh/.github/.github/workflows/pipeline-golang-app.yml@5c377e5944acf613950dfe83a13cc7ab3054728f # main
    secrets: inherit
    with:
      imageTagName: ghcr.io/platform-mesh/extension-manager-operator
      useTask: true
      useLocalCoverageConfig: true
      repoVersionUpdate: platform-mesh/helm-charts
      apiPackagePath: api